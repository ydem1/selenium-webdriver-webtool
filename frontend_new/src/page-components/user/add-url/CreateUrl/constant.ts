import { TextInputType } from "src/@types/textInput";
import { UrlInitialValues } from "src/@types/initialValues";

export const textInputs: TextInputType[] = [
  {
    name: "url",
    label: "URL"
  }
]

export const initialValues: UrlInitialValues = {
  url: "",
};